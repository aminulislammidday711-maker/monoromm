import {Router} from 'express'; import {push,pull} from '../controllers/syncController.js'; import {finalizeDay} from '../controllers/dailyReportController.js'; import {requireAuth} from '../middleware/auth.js'; const r=Router(); r.use(requireAuth); r.post('/push',push); r.get('/pull',pull); r.post('/finalize-day',finalizeDay); export default r;